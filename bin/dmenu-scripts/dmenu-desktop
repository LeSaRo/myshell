#!/bin/sh
# Script to use j4-dmenu-desktop with dwm (ironicaly)

norb="${1:-"#000000"}"
norf="${2:-"#bbbbbb"}"
selb="${3:-"#1b18dd"}"
self="${4:-"#eeeeee"}"

j4-dmenu-desktop -d "dmenu -p 'Launch:' -b -i -fn \"Monaspace Krypton:size=11\" -nb '$norb' -nf '$norf' -sb '$selb' -sf '$self' -c -l 10"
